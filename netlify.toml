[build]
  base = "frontend"              # 👈 Your React app is inside /frontend
  command = "npm run build"      # 👈 Build command
  publish = "frontend/build"     # 👈 Where Netlify should serve files from

[build.environment]
  REACT_APP_API_BASE_URL = "https://customer-churn-prediction-3-9o9v.onrender.com"  # 👈 Replace with your Render backend URL
